# Backend

Hier kommt das Backend für die Aktion-Weihnachtspäckli-App hin.

Railway Root Directory für den Backend-Service:

```text
/backend
```

Der bisherige Backend-Endpunkt im Frontend zeigt auf Railway. Wenn das Backend hier neu aufgebaut oder als neuer Railway-Service daneben erstellt wird, danach `API_BASE` in `../frontend/index.html` auf die aktive Railway-Domain setzen.
# awp_backend
